{
  "name": "docusaurus-link-crawler",
  "version": "0.1.1",
  "description": "Tool to crawl Docusaurus sites and check for broken links",
  "main": "index.js",
  "scripts": {
    "check": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "docusaurus-link-crawler": "./index.js"
  },
  "private": true,
  "dependencies": {
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "puppeteer-core": "^24.8.0"
  }
}
